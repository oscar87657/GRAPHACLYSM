# GRAPHACLYSM 에이전트 작업 지침

## 참고작 조사 · 사용자 명시 요구

사용자가 특정 게임·시스템·자료를 참고하라고 지정하면 구현 전에 해당 자료를 실제로 조사한다. UI 요구는 화면까지 확인하고, 검색 요약·직접 본 화면·검증하지 못한 동작을 구분한다. 원작에서 확인한 사실과 GRAPHACLYSM에 맞춘 설계를 기록하며, 조사 없이 기억만으로 구현하고 참고했다고 표현하지 않는다.

## 최신 · 하나로 이어진 탑 지도 · UI v42

전체 층을 절대 Layer 기준의 세로 지도에 연결. 층 필터/나머지 좌표 제거, 보스→다음 층의 기존 경로 유지. 드래그/휠/스크롤바와 현재 위치/꼭대기/입구 이동. 탑 배경의 긴 기둥·발판·층 경계가 방과 같은 스크롤로 이동한다. 기존 그림 재질과 코드 기반 건축 구조를 사용한다. 진행/저장/전투 규칙41·계정4는 불변. View 수명: 지도당 노드 문자열 배열2개(NodeCount), 층 문자열 배열1개(FloorCount); 지도 교체 때만 할당. [1차 정리와 검증 범위](Docs/PROJECT_CHECKPOINT_V42.md). 후속 기능은 사용자 지시 후 결정.

## 이전 · 잔광 24노드 확장 · 규칙41

잔광 14→24노드(26→36단계). 기존 투자/잔액/분대/비용 보존, 추가 초기화 없음. 후속 체력·회복·공명·보호막·시작 은화·작도 피해·추가 열쇠·시작 손패를 실제 원정에 연결한다. 단일 성좌 드래그/휠/스크롤바/중앙·선행 이동. 계정 v4는 이전14노드 v3의 초기화 완료 상태를 보존하며, 원정 규칙41 준비 비트 확장으로 진행 원정은 불변. 상세: [잔광 확장](Docs/LEGACY_EXPANSION_V41.md).

## 이전 · 잔광 성좌 UI·투자 초기화 · v40

사용자 제공 문화 비교 이미지를 직접 참고해 중앙 마름모 핵과 좌우 연결된14항목 성좌로 교체한다. 탭/상품 목록 제거, 노드 클릭 상세·습득 버튼·선행 노드 이동. 기존 실제 선행/수치/비용 유지. 영구 저장 v3에서 이전 투자를 한 번 전액 환급하고 단계/분대 개방 초기화, 종료 기록과 진행 원정 스냅샷 보존. 전투/원정 저장 규칙39는 유지한다. [구현·복구·검증](Docs/LEGACY_TREE_V40.md).

## 최신 · 즉시 파편 선택·잔광 원정 준비 · 규칙39

새 원정은 연구권 대신 색별 확률이 다른 파편 선택 보상2개를 즉시 사용한다. 상점 연구권도 파편 선택 상품으로 교체. 가로 한 줄·수령 후 제거 유지. 잔광 기존6항목 보존, 작도 피해·시작 은화·유물 지참·열쇠·보상 색 상승·분대3개 개방 추가(총14항목). 기본 탐사/돌파/수호/발굴 중 출발 때1분대. 영구 강화는 누적, 분대는 배타 선택. 원정 준비는 규칙39 명령 스냅샷, 이전 원정 불변. 영구 저장 v2는 안정 ID, v1 여섯 단계·잔광·정산 기록을 보존해 읽는다. `Docs/PREPARATION_V39.md`가 이전 미구현 잔광 제안보다 우선한다.

규칙39 최종 검증: EditMode578/578 (`Logs/preparation39-verified.xml`), 화면23장·게임 Runtime0 (`Logs/preparation39-verified-smoke.log`), 원본/검증 사본 C# 차이0. 즉시 선택·실제 상점 경로 저장/구매 복귀·구형 계정 보존 포함. 중첩 GUI 보상 버튼은 회전 Border 대신 Fill 테두리를 사용한다. 전체5층 밸런스·새 실행 파일은 별도.

## 최신 · 한 줄 보상·무료 시작 유물 정정 · 규칙38

보상은 세로 카드 한 줄, 드래그/휠/가로 스크롤바/좌우 버튼 탐색. 수령·넘김·상자 시도 완료 카드는 숨기고 빈자리 정리, 원래 보상 인덱스/저장 상태는 보존한다. 드래그 끝 수령 방지. 새 출발·재시작은 무료 유물 없이 시작하며 기존37 원정 유물은 보존. 시작3점·총9점·열쇠1개 유지. `Docs/REWARD_ROW_V38.md`. 시작 유물은 잔광의 한 항목이라는 사용자 의도를 따른 별도 미구현 기획: `Docs/LEGACY_REWORK_PROPOSAL_V38.md`. 잔광 구매/환급/정산 코드는 이번에 바꾸지 않는다.

## 최신 · 시작3점·선택 전리품·유물 상자 · 규칙37

새 원정 ExpeditionSupplies는 시작3점+진행최대6점(총9점), 비보스5승마다1점 최대2/1~4층 보스마다1점을 준다. 방 통과 무료점수 제거, 성장 이벤트1점·최소체력6/상점24은화. 무료 시작 유물3택1·열쇠1개. 은화/연구권/카드/유물/열쇠/상자는 카드형 전리품에서 수령하며 남은 보상을 두고 갈 수 있다. 열쇠 확정/직접50% 상자는 한 번만 시도, 재접속 재추첨 없음. 기존36 이하 원정 보존. 잔광 출발점 투자는 새 원정 성장 경험으로 전환하고 기존 계정 단계/재화 보존. 상세·검증·한계: `Docs/EXPEDITION_SUPPLIES_V37.md`.

## 최신 · 최대 성장9점·트리 배치 · 규칙36

새 원정 GrowthLimit은 투자+잔여 합계 최대9점(시작6 유지). 반환/재배분으로 추가 지급 여유를 만들지 않는다. 모든 지급은 RunGrowthState.AddPoints에서 제한하고 방 보상은 실제 지급량만 표시한다. 이전35 이하 원정은 예산 보존.16노드 트리는 세 줄기 직접 연결·간격 조정·일반 이름 표시·각성 구분. `Docs/GROWTH_LIMIT_V36.md`.

## 최신 · 네 궁극기 유지·16노드 성좌 · 규칙35

사용자 결정으로 ZIP은 참고만 사용한다. 궁극기를 캐릭터별로 합치지 않는다. 새 원정 StyleTree는 방식별16노드(무료1+강화9+각성6), 기술/궁극 각성 각각1개. 기존 조건부 상태45개를 새 트리에서 제거하고 과거55노드는 저장 재생용으로 보존한다. 상세/한계: `Docs/STYLE_TREES_V35.md`. 5층·성장점 지급·유물/방 구성은 이번 범위에서 유지. 전투수명 도안2/적 피해배열1 추가, 예측과 실제에 공유.

## 최신 · 전투방식별55노드 · 규칙34

새 ApproachTree 원정은 집행/기록/조율/관측별55노드(무료뿌리1+전용변경9+조건부상태45)를 적용한다. 하나의 뿌리·상향·직계자식최대3·주요 양자택일. 습득한 효과는 다음 전투부터 자동 적용된다. 기존 원정은 자동 이관하지 않는다. 아래 ‘새 트리 미구현’ 기록보다 이 절이 우선한다. 상세: `Docs/APPROACH_TREES_V34.md`.

BattleSpecialization은 전투 수명 bool55/bool37 배열과 정수 조건 마스크를 사용한다. 본 방출 전 조건을 고정하고 후속 상태로 재평가하지 않는다. 수치 설명은 Refresh 캐시, 트리 위치는 레이아웃 생성 시 계산. 사용자 Editor/저장 보존.

프로젝트 전체에 적용한다. 작업 전에 이 문서와 루트 기획·구조 문서를 읽는다. 현재 코드와 최신 사용자 요청이 문서보다 우선한다. 기능 변경 시 문서도 같은 작업에서 갱신한다.

## 최신 · 궁극기 선행 개편·보스 추첨 · 규칙33 · 2026-09-13

새 BattleRework opt-in: 집행 파열/돌진 초기화, 기록2회 재현, 조율 추가 조립, 관측 도안 성역. 기존 수치형 궁극기 중복 적용 금지. 각 층 기존/신규2후보 중 시드로 고정 추첨, 보스 총10종. 새5종은 기존 판정 조합이다. 이전32 이하 저장 유지. `Docs/BATTLE_REWORK_V33.md` 우선. 루나 선택 대사를 이안과 같은 형식으로 수정.

사용자의 다음 성좌 방향은 집행/기록/조율/관측별50~60노드 전용 나무다. 이전 캐릭터당144노드 유지 방침보다 우선한다. 하나의 뿌리·상향 경로·중요 갈림길 배타·모든 유료 노드 실제 효과. 이번 단계는 요청 순서대로 궁극기부터 구현했으며 전용 트리는 아직 교체하지 않았다. 새 원정의 이전 궁극기 노드는 구매/장착 차단하여 무효 투자를 막는다. 이전 저장에는 적용하지 않는다.

추가 조립은 ResolveEnemy 명령에서 적 행동/드로우/턴 진행 없이 조립만 연다. 보류된 첫 드로우는 두 번째 방출/포기 후 실제 적 행동 뒤 지급한다. 상태·이동·기술 대기를 추가 갱신하지 않는다. 사용자 Editor/저장 보존, 격리 사본 검증.

최종 `Logs/rework33-tests.xml`479/479, `Logs/rework33-smoke.log`417화면·게임 Runtime errors0. 네 궁극기의 진행 단계별 저장 재생/추가 조립 포기·보류 드로우,32시드 후보 다양성, 과거 원정 호환 검사 포함. 원본/사본 C# 차이0.720p 추가 조립·성역 직접 확인. 전체5층 수동 밸런스·Profiler·새 실행 파일은 별도.

## 이전 · 5층 원정 · 규칙32 · 2026-09-13

새 원정 FiveFloors opt-in으로 40방·5층, 일반 적2·보스2 추가(총24유형). 격자/가로 띠/체크무늬는 EnemyFieldPatterns의 사각형을 예고와 실제 판정에 공유한다. 기존31 이하 원정3층 보존. 4/5층 비전투 방과 경로는3층 구조 재사용, 새 보스는 장애물/호위 없이 등장. `Docs/FIVE_FLOORS_V32.md` 우선. 성좌는 이번 범위 아님. EditMode464/464 통과, 사용자 Editor/저장 보존.

최종 검증 `Logs/five-floor-tests.xml`:464/464, 실제 체력 감소/교차점 단일 피해 포함. `Logs/five-floor-smoke.log`:375장·게임 Runtime errors0. 새 보스 격자/체크무늬 직접 확인. 도움말 보상 문구와 체력 피해 회귀 추가 뒤 재검증 완료, 원본/검증사본 C# 차이0. 전체40방 수동 밸런스·Profiler·새 빌드는 별도.

## 이전 · 키워드 클릭·성좌 재설계 착수 · 2026-09-13

최신 전체 검수: 카드 상세 제목=대상, 본문=효과명·수치, 하단=기간으로 중복 제거. 경쾌 +0.9 설명은 키워드에만 유지. 현재 유물의 경쾌/집중/균열/고정 중복 풀이와 개발자 비교 문구 제거. 글자 lineHeight가 표시 영역보다 크면 전부 버리던 문제를 교집합 clipping으로 수정. BasicsV9Smoke에105카드 효과·100유물 본문 전체 링크/중복 검수와 짧은 잔불 행 회귀를 추가. `Docs/KEYWORD_CLICK.md` 첫 절 우선. 효과 수치/원정 규칙은 유지한다.

전체 검수 최종: `Logs/keyword-catalog-audit.log` 화면361장·Runtime0, `Logs/keyword-audit-tests.xml`457/457, 원본/사본 C# 차이0. 사용자 저장/Editor 보존.

최신 후속: 회복/수식 자동 밑줄 제외, 버프·디버프/핵심 용어 설명 축약. 설명 제목과 같은 용어 및 동의어는 본문 링크 제외. 문단 캐시 키에 ExcludedTitle 추가, 팝업 최소118. 경쾌 과거/현재 의미 구분. `Docs/KEYWORD_CLICK.md` 첫 절 우선. 전체 능력 연결·자기 링크/동의어 제외 OnGUI 회귀 추가.

후속 최종 `Logs/keyword-brief-smoke.log`: 화면361장·게임 Runtime errors0, 원본/사본 C# 차이0. 용어 연결·자기/동의어 제외·실제 단어 클릭 검사 통과. 이번에는 UI와 문구만 변경, 전체 EditMode 재실행/새 빌드는 별도다.

후속 사용자 이미지 수정: 확대 카드 효과 문장도 DrawExplainedText로 표시한다. 줄 전체 RegisterKeyword 제거, ‘약화’ 등 단어에만2px 밑줄과 클릭 영역. keepsCard/owner를 전달해 확대 카드 유지. 실제 약화 카드의 단어/비단어 클릭 회귀를 BasicsV9Smoke에 추가했다. 새 성좌 작업으로 범위를 넓히지 않는다.

카드 확대/상세·유물·적 사전·성좌 상세의 밑줄 키워드는 클릭으로 설명 고정, 내부 용어 재클릭, ×/Esc/바깥 클릭 닫기를 지원한다. 바깥 누름·뗌은 모두 소비해 뒤 카드/구매 입력을 막는다. 행동 버튼의 기존 동작과 호버 안내는 유지하고, 겹친 작은 손패는 카드 선택을 우선한다. `Docs/KEYWORD_CLICK.md`. 전투/성장/저장 규칙31 유지.

성좌 후속 명세는 `Docs/GROWTH_REDESIGN_BLUEPRINT.md`: 단일 상승144노드 규모 유지, 무료 출발 기록, 미구현 통행료 제거, 실제 첫 확장과 네 궁극기 순차 연결. 아직 새 경로·능력을 적용하지 않았다. 기존31 이하 저장 재생을 보존하는 새 원정 전용 규칙부터 구현한다.

키워드 최종457/457 EditMode (`Logs/keyword-final.xml`), 화면361장·게임 Runtime errors0 (`Logs/keyword-smoke-final.log`), 원본/사본 C# 차이0. OnGUI 클릭/고정/내부 이동/닫기 입력 회귀 포함. 촬영 후 두 설명 문장만 보완하고 EditMode 재검증. 새 빌드·Profiler는 별도다.

## 이전 · 적 사전·층 보스·유물100 · 규칙31 · 2026-09-13

새 출발 TowerArchive opt-in으로 적20유형(기존11+일반6+보스3), 층별 마지막 고유 보스, 유물100종(기존24 조정+19조건×4조합)을 적용한다. 이전21~30 배치/유물은 보존. `Docs/TOWER_ARCHIVE_V31.md` 우선. 유물 과거 풀은 Version30, 현재 All은100. 적은 AttackShape로 예고/실제 판정 공유, 보스 단계는 턴 시작에 고정. 조건부 유물은 본 방출 직전 조건 스냅샷으로 후속 상태를 적용하며 재귀 평가하지 않는다. 사용자 Editor/저장 보존. 최종 검증은 해당 문서에 기록한다.

최종457/457 EditMode (`Logs/archive31-final.xml`), 화면359장·게임 Runtime errors0 (`Logs/archive31-smoke.log`), C# 원본/사본 차이0. 전체3층 수동 밸런스·Profiler·새 실행 파일은 미검증. 새76 유물은19조건×4보상 조합, 적 사전은 훈련용 이름 변형이 아닌20행동 유형 기준이다.

## 이전 · 상태 연계 · 규칙30 · 2026-09-13

새 출발 StatusRules opt-in. 현재 All/Current는 상태 연계 카드, 이전29 효과는 Version29/MarketCard로 보존한다. 유도/견인 카드/상처 연계, 수호/관통/방벽 약화, 집중 통합과 경쾌 거리 증가. 새 규칙 포수의 사선/고리는 몸과 함께 이동하고 지면 조준은 유지한다. `Docs/STATUS_RULES_V30.md` 우선. 사용자 저장/Editor 보존. 최종 검증은 해당 문서에 기록한다.

최종372/372 EditMode (`Logs/status30-final.xml`), 화면247장·게임 Runtime errors0 (`Logs/status30-final-smoke.log`), C# 원본/사본 차이0. 전체 수동 밸런스·Profiler·새 실행 파일·카드 견인 후 전체 연쇄 위치 예측 UI는 별도다. 상태 ID12~16은 새 상태이며 카드 능력 ID14~18과2 차이가 나므로 단순 enum 캐스팅을 추가하지 않는다. Pull은19번 즉시 능력이다.

## 이전 · 카드 설명 구획화 · 규칙29 유지 · 2026-09-13

- 후속 요청으로 카드 앞면 맨 아래 뽑기/위력 배지와 전용 구분선을 제거했다. 작은 카드 설명 공간 확대. 실제 드로우 효과·상세 위력·전투/저장 규칙은 유지한다.

- 효과별 대상/내용/지속시간 구획과12px 여백, 도안 설명 우선·수식 보조로 바꾼다. 상점 상세·도감·전투·보상·보유 덱에서 공유. 확대 카드에는 세로 요약2행과 기존 키워드 상세를 유지한다. `Docs/CARD_READABILITY_V29.md`.
- SkillVisual에 현재/과거 실제 카드별 표시 문자열을 초기화 시 캐시. 재사용 스타일4개/GUIContent1개, 글꼴 높이 측정, 새 프레임 문자열 조립 없음. 확대 손패340×420, 입력 통로 유지. Core/효과/확률/가격/저장 변경 없음.
- 최종359/359 EditMode (`Logs/cardread29-tests.xml`), 화면242장·게임 Runtime errors0 (`Logs/cardread29-final-smoke.log`).105종 도감 효과 영역 초과1px 이하 검사, 손패/상점720p/도감 직접 확인. C# 원본/사본 차이0. 수동 취향·Profiler·새 실행 파일은 별도.

## 이전 · 카드 효과·다품목 상점 · 규칙29 · 2026-09-13

-105종 색 분포21/21/42/21. 새 원정 MarketBalance opt-in에서 등급별 보상50/28/18/4%, 상점40/30/24/6% 색 선추첨. 색 안에서 미선택 카드 균등, 카드 가격8/12/16/20, 위력0~1/2/3/4 및 부가 효과 차등.
- 카드5·유물3·기존 정비4·연구권1=13상품. 실제 앞면/그림과 별도 구매 버튼, 클릭 상세. 카드/유물 ID 중복 없음, 삭제 확정 결제 유지. `Docs/CARD_MARKET_V29.md`.
- All/Current는 새 균형 카드, Version28/Previous는 이전105종, Find는 과거용. 과거21~28 저장은 새 효과·가격·추첨·13상품을 소급하지 않는다. BattleSession/UI는 CardDefinition.WeavePower 사용. Visual은 정확한 카드 참조로 과거/현재 효과를 구별한다.
- 형식2/규칙29. MarketBalance는 GrandArchive 뒤 새 출발에만 허용. 기존 사용자 저장/Editor 보존. 은행/재고 갱신/상인 전투/유물 새 등급은 이번 범위가 아니다.
- 최종359/359 EditMode (`Logs/market29-final.xml`), 화면207장·게임 Runtime errors0 (`Logs/market29-smoke.log`), 원본/사본 C# 차이0.720p 진열/상세 확인. 전체 원정 수동 밸런스·Profiler·새 실행 파일은 미검증.

## 이전 · 카드 색 재검토 · 2026-09-13

- 전체105종 중39종 색 변경: 회색12/청록19/보라37/금색37. 일곱 현의 서곡·오중 직조 금색, 단순 반사/크기/고정 회전 회색. 부가 정화 자동 금색 제거. `Docs/CARD_COLOR_REVIEW.md`에105종 전후 표.
- `CardDefinition.DiagramRarity`가 UI와 색 필터 기준. 기존 `Rarity`와 프로필 `Tier`는 가격/보상 추첨/위력 및 규칙28 저장 재생 보존용이다. 두 값을 다시 합치지 않는다. 기존 저장도 최신 색 표시, 이번에는 가격·추첨·피해·도안 변경 없음.
- 후속 사용자 피드백으로2차 분류할 검토안이며 전체 조합 밸런스 확정이 아니다.
- 최종344/344 EditMode (`Logs/regrade28-tests.xml`), 화면204장·Runtime errors0 (`Logs/regrade28-smoke.log`). 일곱 현의 서곡 금색 직접 화면 확인. C# 원본/검증 사본 차이0. 사용자 Editor와 저장 보존.

## 이전 · 네 색의105종 카드 · 2026-09-13

- 사용자 요청으로105종(기존41+새64) 실제 카드 풀과 네 색 분류를 추가했다. 새64종은16개 합성 연산 계통의 주기/축/효과 변형이며 단순 이동 추가가 아니다. `Docs/WEAVE_ARCHIVE_V28.md`.
- 카드·상점·보상·도감의 등급명은 제거하고 테두리/띠/문양 색으로 표시한다. 재를 걷는 바람·유성 보폭은 금색. 도감 색 필터/빈 조합 안내 지원. 전투 피해 순위가 아닌 도안/활용 변화 기준이다.
- 규칙28 GrandArchive는 ContentExpansion 뒤 새 출발에서만105개 풀과 재분류 시작 덱을 채택한다. Legacy35/Version27의41개 원본 정의 및 Find를 보존하고 All/Current가 새105개 정의를 제공한다. 이전21~27 저장에 새 RNG/가격을 소급하지 않는다.
- AdvancedWeaves는 기존1536표본 prefix 버퍼로 이전 F(t) 전체에 작용한다. 주파수96/조립8/취소/실제 Sample 판정 유지. 신규 문양은 View 소유 수식 버퍼 하나로 초기화한다. 새64개가64개 독립 전투 시스템이라고 과장하지 않는다.
- 전체 수동 밸런스·새 Windows 빌드·Profiler는 미검증. 성좌 전면 개편은 이번 범위가 아니다. 사용자 Editor와 저장은 보존한다.
- 최종332/332 EditMode (`Logs/archive28-final.xml`), 화면204장·게임 Runtime errors0 (`Logs/archive28-smoke.log`). 신규64종 전체의 첫 도안/설명/문양과 네 색 필터/빈 조합 확인, 원본/사본 C# 차이0.

## 이전 · 상점 진열·콘텐츠 확장 · 2026-09-12

- 상점에 실제 카드 앞면/유물 그림과 별도 구매 버튼을 배치했다. 카드6종(총41), 조건부 유물4종(총24), 고리/십자/쌍점 공격3종과 조합3개(총11) 추가. `Docs/CONTENT_AND_SHOP_V27.md`.
- 규칙27 ContentExpansion 명령은 새 방식 원정의 경제 활성화 직후에만 기록한다. 기존21~26은 Legacy 카드35/유물20 풀과 기존 생성 규칙을 보존한다. All에는 새 콘텐츠가 있으며 도감에서 전체 표시. 카탈로그 참조를 무심코 All로 교체하면 과거 저장 RNG가 바뀐다.
- 응축 유물 조건은 TryBeginPlot의 CondenseCount 초기화 전에 저장한다. 고리/십자/쌍점 조준은 턴 시작에 고정되며 예측과 실제 피해가 같은 EnemyState 판정을 사용한다. 신규 유물은 본 방출에서만 평가하고 후속 복제에서 재발동하지 않는다.
- 사용자 Editor/저장 보존, 격리 사본 검증. 전체 원정 수동 밸런스·Profiler·새 실행 파일은 미검증. 성좌 전면 개편은 별도 후속이며 이번에 완료했다고 표현하지 않는다.
- 최종322/322 EditMode (`Logs/content-v27-verified.xml`), 화면135장·게임 Runtime errors0 (`Logs/content-v27-smoke-final.log`), 원본/사본 C# 차이0. 고리 경고의 GUI 그룹/회전 충돌은 월드 선분 ClipToField 방식으로 수정하고 최종 화면에서 재확인했다.

## 이전 · 전리품·상점·전장 분리 · 2026-09-12

- 새 원정 규칙26: 승리 은화·연구권, 도입 이후 일반전 25% 추가 유물, 층별 상점과 카드/유물/회복/훈련/제거/성장점 구매. 연구권은 지도에서 카드 선택으로 사용한다. 기존21~25 저장은 예전 규칙을 보존한다. `Docs/EXPEDITION_ECONOMY_V26.md`.
- 같은 행 방 내용 중복을 줄이고 기존 적 행동을 8개 전투 조합으로 확장했다. 필드 비율을 유지하며 손패 위로 분리하고 기술 조준 중 카드 상세·오래된 팝업 입력 차단을 해제한다.
- 성좌의 실제 효과와 연결 대기를 구분한다. 새 원정의 루나 쌍성 핵심 장착은 관측 방식으로 전환한다. 240개 효과 전체 구현이 아니며 다음 실제 효과/선행 경로 개편은 `Docs/GROWTH_REWORK_NEXT.md`를 따른다.
- 격리 사본 EditMode 309/309 (`Logs/economy-final.xml`), 화면122장·게임 Runtime errors0 (`Logs/economy-smoke-final.log`), 원본/사본 C# 차이0. Unity SearchDatabase 시작 예외는 별도 Editor 서비스 오류다. 전체 3층 수동 밸런스·재미 측정·Profiler·새 Windows 빌드는 미검증. 사용자 Editor/저장 보존.

## 이전 · 첫 원정 흐름 · 2026-09-12

- 사용자 ‘다음 진행’에 P4의 1층 페이싱 구현. 새 방식 원정은 도입/정비/활용/돌파·정예/보상/복합전/보스 준비 순서. 무료 첫 공방 훈련, 약한 무리 체력12, 첫 승리2점·첫 보스3점으로 기본 최종 보스 전35점 유지. `Docs/OPENING_ROUTE_V25.md`.
- 형식2/규칙25, 끝에 OpeningRoute 명령 추가. 새 출발/재시작에서만 활성화. 기존21~24 이어하기·핵심 체험·2/3층·기술/판정 유지. 진행 중 지도 교체 금지. 궁극기·240노드 전체 개편 완료가 아니다.
- 검증은 격리 사본과 전용 저장 폴더. 수동 재미·전체 3층 밸런스·Profiler·Windows 새 빌드는 미검증. 다음 확장 후보는 단순한 궁극기의 운용 차별화이며 먼저 실제 전투로 검증한다.
- 최종 EditMode 296/296 (`Logs/opening-route-final.xml`), 화면112장·게임 Runtime errors0 (`Logs/opening-route-smoke-final.log`). 조율형 두 전투 실제 진행·정비 후 이어하기, 네 방식 첫 승리 회귀, 원본/사본 C# 차이0. 시작 중 Unity SearchDatabase 내부 예외는 별도 Editor 서비스 오류다.

## 이전 · 핵심 노드 비교·체험 · 2026-09-12

- 사용자는 전투 기술·판정에 만족하고 우선순위대로 진행을 요청했다. P3로 네 핵심(집행/기록/조율/관측)의 실제 전후 비교와 원정에서 나갔다 돌아오는 체험을 추가했다. `Docs/CORE_NODE_TRIALS.md`.
- 잠긴 핵심도 지도·휴식에서 체험 가능. 원래 원정 객체와 성좌 카메라/선택 보존, 자동 습득 없음. 체험 진입 전 저장, 체험 중/앱 종료 저장도 원래 원정 대상. 종료/재시작/승패 후 복귀를 지원한다. 형식2/규칙24 유지.
- 최종284/284 EditMode (`Logs/node-trials-final.xml`), 화면104장·게임 Runtime errors0 (`Logs/node-trials-smoke-final.log`). 저장 파일 내용·원정 객체·G 복귀/방출 중 종료·성좌 카메라 보존 검사. 전체 노드·각성·궁극기 시연 완료가 아니다. 궁극기는 단순하다는 피드백을 후속 재설계 과제로 남겼고 이번 전투 규칙/비용은 변경하지 않는다. 수동 재미·전체 원정 밸런스·Profiler·새 Windows 빌드는 미검증.

## 이전 · 관측 루나 출발 방식 · 2026-09-12

- 사용자 의도는 전투 안 J 추가가 아니라 이안처럼 루나의 두 번째 시작 방식이었다. 조율/관측 두 출발 선택, 네 번째 체험과 원정/재시작을 연결했다. `Docs/OBSERVATION_LUNA_V24.md` 참조.
- 관측 K: 지속 위성 하나 배치, J: 빈 조립에서 다음 식 원점 몸/위성 전환. 위성 또는 몸 자가 적중을 단일 효과로 처리한다. 기존 식은 위성 이동으로 바뀌지 않고 귀환점은 몸으로 돌아온다. 관측형에는 조율/견인이 없다.
- 형식2/규칙24, 기존21~23 저장 유지. 새 위성 명령만 추가. 전용 하위 성좌/각성 전체 구현은 아니다. 사용자의 Editor/저장 보존, 격리 사본 검증.
- 최종272/272 EditMode (`Logs/observation-final.xml`), 화면87장·게임 Runtime errors0 (`Logs/observation-smoke-final.log`). C# 원본/검증 사본 일치. 수동 재미·3층 밸런스·Profiler·새 Windows 빌드는 미검증.

## 이전 · 방향 돌진·성운 견인 · 2026-09-12

- 집행형 이안은 K → 방향 클릭으로 3.6 관통 돌진. 실제 경로만 공격한다. 지형·경계·봉인 제한과 파열/처치 재사용 유지.
- 조율형 루나에게 J 성운 견인 추가. K 도안 조율과 합쳐 턴당 한 번. 도착점/도안 접촉 미리보기, 포수의 고정 사선 보존. `Docs/SPATIAL_SKILLS_V23.md` 참조.
- 형식2/규칙23, 21/22 읽기 유지. 과거 대상 명령은 변경하지 않고 ExecutionDash/LunaPull을 추가했다. 262/262 EditMode, 최종 화면81장·Runtime errors0 (`Logs/spatial-skills-smoke-final.log`). 사용자 저장과 Editor 보존. 수동 밸런스·Profiler·새 Windows 빌드는 미검증.

## 이전 · 전투 방식 원정 연결 · 2026-09-12

- 체험에 대한 사용자의 긍정 피드백 후 세 방식을 실제 원정에 연결했다. 캐릭터 선택에서 집행/기록 이안·조율 루나로 출발. 6장 덱/손패와 동일 배치 도입전 후 기존 3층 지도/보상/성장을 진행한다. 상세는 `Docs/APPROACH_CAMPAIGN_V22.md`.
- 핵심 기록자 장착으로 다음 전투의 집행/기록 전환. 파열 집행, 먹빛 필사, 역위상 조율을 실제 연결했다. 원본 카탈로그를 전체 구현처럼 표시하지 않는다. 다른 전투 기술 슬롯/노드는 여전히 부분 구현이다.
- 형식2/규칙22, 21 저장은 기존 규칙으로 읽는다. ChooseApproach/DiagramAbility 명령을 끝에 추가. 조준/실행/저장 좌표 0.01 단위 일치. 연습 분리는 유지한다.
- 최종 245/245 EditMode, 화면 73장·게임 Runtime errors 0. 로그 `Logs/approach-campaign.xml`, `Logs/approach-campaign-smoke-final.log`. 전체 원정 수동 밸런스·Profiler·Windows 새 빌드는 미검증. 사용자 Editor와 저장을 건드리지 않는다.

## 이전 · 한 번의 작도 전투 비교 · 2026-09-12

- 메인에서 바로 플레이하는 집행 이안/기록 이안/조율 루나의 동일 손패·전장 비교를 구현했다. `Docs/ONE_PLOT_COMBAT.md`가 현재 범위다. 전체 원정·240노드 개편 완료로 취급하지 않는다.
- `BattleApproaches`가 도안 이동/회전, 다음 방출 기록 공격, 동력선 절단을 소유한다. 실제 표본과 예측을 공유하며 도안 복제는 카드 능력 재사용과 분리한다.
- `IsPractice`는 저장/종료 잔광/성장 보상 없음. 일반 원정 시드와 기존 이어하기 보존. 일반 형식2/규칙21 유지. 원본 Editor를 종료하지 않는다.
- 231/231 EditMode, 최종 화면 64장·게임 Runtime errors 0. 로그 `Logs/signature-tests.xml`, `Logs/signature-smoke-final.log`. 재미·전체 전투 승률·전체 원정 개편·새 실행 파일은 미검증/미완료다.

## 현재 작업 · 성장 UX 추가 기획 1차

- 혼합형 상승 나무를 유지하며 240개 기능명/설계 요약·검색·선행 비용/조건 비교를 추가했다. 경로 창은 읽기 전용이며 일괄 결제는 아직 없다.
- 영구 성장 화면은 목표 단계 편집→내역 확인→확정. 후보 저장 성공 후에만 실제 잔광/rank 채택. 손상 영구 기록은 읽기 전용으로 원본 보존.
- 전투 수치·성장 예산·원정 형식2/규칙21·영구 형식1 유지. 218/218 EditMode 통과. `Docs/Overhaul/GROWTH_UX_PROGRESS.md`의 미완료를 먼저 확인한다. 실제 아이콘·반응형 상세·영구 환급/원장·종료 보상 스냅샷·배치 경로 명령·출발 계획은 미완료다.

## 현재 작업 · Overhaul P0 선행 구현 · 2026-09-12

- 사용자 제공 Overhaul을 현재 v20에 단계적으로 구현한다. `Docs/Overhaul/BASELINE_DIFF.md`, `WORK_PLAN.md` 참조. v15로 되돌리지 않는다.
- `CombatSkillPreview`가 기술 예측/실행의 착지·접촉 계산을 공유한다. 잘못된 명시 대상과 착지 실패는 거절한다. 조준 중 착지/적중/HP·보호막 피해 표시. 기존 주 대상 별도 타격과 2.8 이동 한도는 유지한다.
- 저장 형식 2/규칙 21. 기존 저장/설정/영구 기록 보존. 구조 링크·추진·6방 원정·각인·네 빌드는 아직 미구현이며 P0 전부 완료로 취급하지 않는다.
- 새 기준 194/194, 수정 후 208/208 EditMode 통과. OS 입력 전 사례와 사람의 재미 검증은 별도다.

## 현재 작업 · 전투 대응 실험 v20 · 2026-09-12

- 사용자가 전투 재미 기획의 실험 진행을 승인했다. 첫 단계만 구현: 선형 포수(고정 선분 공격), 봉인 서기관(1턴 예고/1턴 이동·프리즘 봉쇄, 실제 그래프로 해제), 연결 수호자(그래프 피해 −4, 본체+중앙 매듭으로 해제).
- 1층 일반/정예를 적 둘의 고정 조합 세 종류로 구성했다. `DungeonGenerator.TacticalEncounter`가 재현 가능한 정의를 제공한다. 보스/2·3층은 기존 전투다.
- `BattleThreats`의 예상 판정과 `GraphaclysmThreatView`의 표시를 공유한다. 주 피해는 방출 전 스냅샷으로 고정한다. 저장 형식 2/규칙 20, 새 원정 필요. 상세는 `Docs/TACTICAL_THREATS_V20.md`.
- 빌드 3종·잔선·카드 보존·특수 승리 조건은 아직 미구현이다. 기존 개별 해제와 손패 입력 수정은 유지한다.
- 검증: EditMode 194/194, 화면 fixture 48장·게임 Runtime errors 0. 전체 원정 수동 플레이·재미 개선 측정·새 실행 파일 빌드는 하지 않았다.

## 이전 작업 · v19 개별 해제·카드 입력 후속 · 2026-09-12

- 지도/휴식에서 개별 노드 전액 반환을 지원한다. 후속 노드의 선행/진입/투자 조건을 깨는 반환은 거부한다. 자동 연쇄 삭제하지 않는다. `RefundGrowth`를 마지막 명령 ID로 추가하고 기존 v19 규칙/저장 재생은 유지했다.
- 손패는 맨 앞 확대/상승 카드 우선으로 단일 입력 대상을 정한다. 그리기 루프에서 카드별 GUI.Button을 만들지 않는다. 누름·뗌 대상이 일치해야 사용한다.
- EditMode 180/180. 상세는 `Docs/NODE_REFUND_CARD_INPUT.md`. 전투 재미는 `Docs/COMBAT_ENGAGEMENT_PROPOSAL.md`에 기획만 작성했다. 이를 구현 완료로 취급하거나 요청 없이 적용하지 않는다.

## 이전 작업 · 성장 가독성·방 선택 v19 · 2026-09-12

- 노드 설명은 클릭으로 고정하고 습득 버튼으로 확정한다. 밑줄 용어만 호버한다. 단일 상승 트리의 5단계 진입은 누적 투자 0/5/10/16/22점이며, 280 간격 열과 직각 연결 통로를 사용한다.
- `GrowthPlayerText`는 원본 기획과 별개로 짧은 플레이어용 설명을 제공한다. 미구현 노드를 구현된 능력처럼 표시하지 않는다.
- 방 9종·이벤트 10종, 같은 행의 종류 다양성과 다음 방 선택을 보장한다. 5점 투자마다 숙련(+그래프 피해 1/시작 보호막 1, 최대 5), 공방 훈련·일회용 보급을 실제 적용한다.
- 적 수치를 상향하고 체력 피해/보호막 흡수를 구분하는 피격 연출을 추가했다. 저장 형식 2/규칙 19로 새 원정 필요. EditMode 176/176. 상세는 `Docs/PROGRESSION_READABILITY_V19.md`를 따른다.

## 이전 작업 · 탐색·전투 다양성 v18 · 2026-09-12

- 하나의 아래→위 트리를 유지한다. 시작 후 선택 3개, 수평 행 최대 6개, 높이 흔들림 제거, 중복 선행 선 생략. 호버 설명을 유지하며 16개 밑줄 용어에 툴팁을 붙인다. 분류별 허브를 다시 만들지 않는다.
- `BattleDevices.cs`: 맥동(짝수 턴 피해 6/작도 봉쇄), 일회용 보호 기점(+5), 파열 결정(반경 2.1 피해 6), 정확한 미리보기. 적 접촉·중첩 허용, 기둥·경계 유지. 격앙/기동 행동과 카드 4종 추가(총 35종), 12장 시작 덱 조정.
- 현재 층 8개 방을 위로 연결하고 `tower-ascent-v18.png`를 로드한다. 전투 방향 버튼과 보조 설명은 상세/호버로 이동했다.
- 저장 형식 2/규칙 18. 새 원정 필요. 240개 노드의 미연결 효과는 그대로다. EditMode 169/169 통과. 범위·조사·아트 프롬프트·화면 검증은 `Docs/ASCENT_PLAYABILITY_V18.md` 참조.

## 이전 작업 · 아래→위 단일 성장 트리 v17 · 2026-09-12

- 사용자 요청은 전투 판도를 바꾸는 정도에 따라 기술·궁극기·공통 규칙이 섞이는 하나의 트리다. 대칭이나 Section별 묶음을 배치 기준으로 되살리지 않는다.
- `GrowthTreePaths`가 실제 진입 조건을 소유하며 `교차점 채집` 하나에서 시작한다. `CanPurchase`가 기존 필수 조건과 진입 후보 중 하나의 습득을 함께 검사한다.
- `GrowthTreeLayout`은 의존성과 변화 크기로 아래→위 높이, 효과 연관성과 부모 위치로 좌우 배치를 만든다. `GraphaclysmGrowthTreeView`가 드래그·휠 이동·Ctrl+휠 확대·오른쪽 상세창을 그린다. 노드 클릭은 선택, 습득/장착은 상세창에서 확정한다.
- 저장 형식 2/규칙 17. v16 원정은 재생하지 않는다. 개별 전투 효과 미연결 범위는 v16과 같다. 상세는 `Docs/ASCENDING_GROWTH_V17.md`다.
- Unity EditMode 160/160 통과. 화면 fixture 38장·게임 Runtime errors 0. 양쪽 캐릭터 연결 방향·노드 겹침과 탐색 입력을 검사한다.

## 이전 작업 · 대형 변형 성좌 기반 v16 · 2026-09-12

- 사용자 제공 `GRAPHACLYSM_성좌_240노드.json`을 재생성 가능한 불변 카탈로그로 편입했다. 공통 48 + 캐릭터별 96으로 이안/루나가 각각 144개에 접근한다.
- `RunGrowthState`는 시작 6점과 직접 성장점, 획득/장착 분리, 핵심 1·서로 다른 기술군 2·궁극기 1, 선행/2-of-3/국소 배타/특성 한도와 전체 재배분을 소유한다.
- 저장 명령은 배열 순번 대신 안정 문자열 ID의 결정적 해시를 사용하며 저장 형식 2/규칙 16이다. 기존 규칙 15 원정은 재생하지 않는다.
- 성장 화면은 탭으로 분리하지 않은 144노드 단일 대성좌다. 기억의 핵에서 공통 규칙·캐릭터 능력·전투 기술·궁극기·혼합 성좌가 하나의 연결망으로 갈라지며, 드래그 이동·휠 확대/축소·전체 보기·선택 노드 이동을 지원한다. `부분 구현`은 v15 대응 기본 형태, `설계 등록`은 획득·장착·저장만 연결되고 전투 효과는 아직 미구현임을 뜻한다.
- 검증: Unity EditMode **156/156**, Full HD/720p fixture **35장**, 게임 Runtime errors 0. Windows 빌드·수동 플레이·전체 런 밸런스·Profiler는 재검증하지 않았다. 상세 계약은 `Docs/LARGE_CONSTELLATION_V16.md`다.

## 이전 작업 · 전투 기술 대상 지정 후속 수정 · 2026-09-12

- 사용자 저장의 전투 기술 명령 8회가 모두 대상 `-1`로 기록된 것을 확인했다. 기존 버튼/K 입력은 적을 고르는 단계 없이 즉시 가장 가까운 적에게 발동해, 사용자가 보고 있던 적과 실제 주 대상이 달라질 수 있었다.
- 버튼 또는 K로 대상 지정 상태에 들어간 뒤 필드 표식이나 오른쪽 적 목록을 클릭해 발동하도록 수정했다. 우클릭이나 기술 버튼 재클릭으로 취소한다.
- 기술이 보호막에 전부 막힌 경우에도 체력 변화가 없어 무피해처럼 보이지 않도록 체력 피해와 보호막 파괴량을 별도로 표시한다. 주 대상의 체력+보호막 감소를 화면 회귀에서 검사한다.
- 저장 명령과 전투 수치는 바뀌지 않아 형식 2/규칙 15를 유지한다. 기존 `-1` 명령은 가장 가까운 적 대상으로 계속 재생하고 새 입력은 명시적 대상 인덱스를 기록한다.
- 검증: Unity EditMode **155/155**, 대상 선택 화면을 추가한 화면 회귀 **34장**, Runtime errors 0.

## 다음 설계 우선순위 · 2026-09-11

사용자가 v15를 직접 확인했고 전체 방향은 좋다고 평가했지만, 다음 작업에서는 아래 항목부터 다시 설계·수정한다. 이번 마감에서는 코드나 수치를 더 바꾸지 않았다.

- 현재 성좌를 완성안으로 취급하지 말고 전면 재검토한다. 스킬 수, 갈래 구조, 스킬 교체 노드와 부가 효과 노드의 구성부터 다시 논의한다.
- 이안과 루나의 전투 스킬 구성 및 각 성좌의 실제 전술 차이를 함께 변경한다. 단순 수치 강화보다 플레이 방식이 달라지는 선택을 우선한다.
- 이동 기술 무피해 현상은 위 2026-09-12 후속 수정에서 실제 저장 입력과 보호막 피드백을 추적해 보완했다. 수동 플레이로 대상 지정 감각은 다시 확인한다.
- 위 수정은 다음 세션 범위다. 2026-09-11 마감 시점에는 v15 커밋 `d0d2028`과 깨끗한 `main`을 기준으로 둔다.

## 재개용 인계 · 전투 조작·기술 성좌 v15 · 2026-09-10

사용자 피드백에 따라 선택 기록의 눈 상태, 조립 목록의 누적 들여쓰기, 사방 고정 이동과 기술 착지 빗나감, 캐릭터가 공유하던 단조로운 성장 계통을 함께 개편했다.

- 선택한 캐릭터는 포인터가 떠나도 눈을 뜬 채 유지한다. 조립 목록은 모든 파편을 같은 x 기준선에 정렬하고, 성좌 화면은 불투명 바탕과 밝은 제목을 사용한다.
- 일반 이동은 턴당 한 번 반경 1.8의 필드 좌표를 클릭한다. 범위 밖은 경계로 제한하고 적·기둥과 겹치면 가장 가까운 빈 지점으로 보정한다. 미리보기 원·도착 마름모와 방향키 빠른 이동, 이동 취소를 제공한다.
- 성좌는 캐릭터별 18노드다. 전투 기술 3형태·궁극기 3형태를 교체하고 각 형태의 부가 노드 둘 중 하나를 택한다. 이안은 관통/처형/거울 전이, 루나는 착지 파동/왕복/연쇄 전이를 사용한다.
- 기술은 안전 착지와 선택 주 대상 적중을 분리한다. 충돌 여백 앞에서 멈춰도 주 대상 피해가 빠지지 않으며 직선·다중선·폭발·왕복·연쇄 연출을 구분한다.
- 원정 저장은 형식 2/규칙 15다. 임의 이동은 0.01 단위 `MoveTo` 명령으로 재생하고 v14 이하 원정은 재생하지 않는다. 상세 계약은 `Docs/COMBAT_CONTROL_GROWTH_V15.md`다.
- 검증: Unity EditMode **154/154**, Full HD/720p fixture **33장**, 게임 Runtime errors **0**. 로그는 `Logs/editmode-growth-movement-v15.xml`, `Logs/growth-movement-v15-smoke.log`; Windows 빌드·수동 플레이·Profiler·전체 3층 밸런스는 재검증하지 않았다.
- 사용자 Editor PID 15320과 그 AssetImportWorker는 종료하지 않았고 격리 사본 `Logs/CombatV2VerificationProject`에서 검증했다.

## 이전 인계 · 전투 정보·성좌 성장 v14 · 2026-09-10

사용자 피드백에 따라 보상 카드 글자 겹침, 끊기는 카드 키워드, 보이지 않는 상태 수치/턴과 카드 목록형 성장 UI를 전면 개편했다.

- 대형 카드는 제목/문양/수식/설명/키워드/위력 구획이 분리된다. 손패·확대 카드·키워드·통로·설명 패널 중 하나에 포인터가 있으면 상세가 유지된다.
- 상태 칩은 축약 이름과 `수치 · 남은 적 행동 T`를 직접 표시한다. 넘치는 상태는 `+N` 툴팁에서 전부 읽는다.
- 원정 성장은 핵→이동 기술/궁극기→형태→하위 변형으로 이어지는 12노드 성좌다. 부모와 같은 갈림길 배타 조건을 Core가 검사한다.
- 오중 궤적, 처치 공명 +1, 궁극기 다중 적중 공명 환급과 캐릭터별 추가 피해·범위·고정·방어·회복이 실제 전투에 적용된다.
- 원정 저장은 형식 2/규칙 14다. v13 이하 원정은 재생하지 않고 설정·영구 기록은 유지한다. 상세 계약은 `Docs/COMBAT_READABILITY_V14.md`다.
- 검증: Unity EditMode **150/150**, Full HD/720p fixture **32장**, 게임 Runtime errors **0**. 로그는 `Logs/editmode-ui-tree-v14.xml`, `Logs/ui-tree-v14-smoke.log`; 시작 중 SearchDatabase 내부 예외는 게임 진입 전 Editor 서비스 오류다. Windows 빌드·수동 플레이·Profiler·전체 3층 밸런스는 재검증하지 않았다.
- 사용자 Editor PID 15320은 종료하지 않았고 격리 사본 `Logs/CombatV2VerificationProject`에서 검증했다.

## 이전 인계 · 전투 흐름·분기 성장 v13 · 2026-09-10

사용자 피드백에 따라 중앙 기준의 빗나감, 끊기는 카드 툴팁, 이동 수치 위주의 카드, 자동 공명, 단순 강화 트리를 교체했다. 탑형 지도와 스토리는 여전히 후속 구상 범위다.

- 빈 조립의 첫 파편은 사용 순간 플레이어 좌표를 기준점으로 삼는다. 유성 보폭·별빛 보루·굴절 보법·성운의 품은 7갈래 폭발·거울 접기·파쇄 회전·5:2 리본의 새 전체 궤적 변환이다.
- 확대 카드 키워드에서 고정 툴팁까지 포인터 통로를 유지한다. 패널/통로/툴팁 위에서는 카드 상세가 닫히지 않는다.
- 기본 전투 기술은 가리킨 적 방향으로 최대 2.5 이동하며 경로 공격, 대기시간 3턴이다. 넓은 세 갈래와 처치 즉시 초기화 직선, 캐릭터별 방어/공격 모듈, 궁극기 변주를 레벨 2/3/4에서 쌍마다 하나만 고른다. 기술 이동은 취소할 수 없다.
- 공명은 자동 +1을 제거했다. 적 2명 이상 적중 +1, 자신·적 동시 적중 +2이고 중첩 가능하다.
- 원정 저장은 형식 2/규칙 13이다. v12 이하 원정 저장은 재생하지 않고 설정·영구 기록은 유지한다. 상세 계약은 `Docs/COMBAT_FLOW_V13.md`다.
- 검증: Unity EditMode **148/148**, Full HD/720p fixture **30장**, 게임 Runtime errors **0**. 로그는 `Logs/editmode-combat-flow-v13.xml`, `Logs/combat-flow-v13-smoke.log`; 배치 시작 중 Unity SearchDatabase 내부 예외 1건은 게임 진입 전 Editor 서비스 오류다. 전체 런 밸런스·Profiler·Windows 수동 플레이는 재검증하지 않았다.
- 사용자 Editor PID 15320은 종료하지 않았고 격리 사본 `Logs/CombatV2VerificationProject`에서 검증했다.

## 이전 인계 · 전투·성장 v12 · 2026-09-10

사용자가 버프·디버프·카드·유물과 캐릭터별 원정 스킬 트리, 패배·완주 재화로 올리는 영구 트리를 요청했다. 탑형 지도와 스토리는 후속 구상 범위로 남긴다. 아래 v11 인계보다 이 항목이 최신이다.

- 상태는 가시·추진·요새화·파열을 더해 12종, FragmentCardCatalog는 31종, FragmentRelicCatalog는 20종이다. 신규 유물 6종은 투명 RGBA 그림을 보상·사전·보유 화면에 표시한다.
- `RunGrowthState`는 방 완료 경험치, 레벨, 성장점, 캐릭터별 6노드와 선택한 전투 기술/궁극기 변형을 소유한다. G에서 열고 K로 전투 기술을 전투당 한 번 사용한다. 새 원정에서 초기화된다.
- `LegacyProgression`은 패배·완주 잔광과 최대 체력·승리 회복·시작 공명/경험/보호막·잔광 보너스를 소유한다. 메인 화면에서 구매하며 `legacy.save`에 영구 보존된다. 같은 종료 시드는 중복 보상하지 않는다.
- 원정 저장은 형식 2/규칙 12이고 영구 효과 스냅샷과 성장 명령을 재생한다. v11 이하 저장을 조용히 호환하지 않는다. 상세 계약은 `Docs/COMBAT_GROWTH_V12.md`다.
- 검증: Unity EditMode **146/146**, Full HD/720p fixture **29장**, Runtime errors **0**. 로그는 `Logs/editmode-combat-growth-v12.xml`, `Logs/combat-growth-v12-smoke.log`; 전체 런 밸런스·Profiler·Windows 수동 플레이는 재검증하지 않았다.
- 사용자 Editor는 PID 15320으로 열려 있었다. 격리 사본 `Logs/CombatV2VerificationProject`에서 검증했으며 사용자 Editor를 종료하지 않았다.

## 재개용 인계 · 캐릭터 선택 v11 · 2026-09-10

사용자가 v10 선택창 의도를 정정했다. 기존 전신 초상 크롭과 확대·블러·하이라이트는 사용하지 않는다. 새 옆모습에서 기본은 눈 감음, 호버는 눈 뜸이다. 아래 v10 인계보다 이 항목이 최신이다.

- 이안은 왼쪽, 루나는 오른쪽을 향하는 선택 전용 옆모습이다. 불투명 검은 원 배경은 없고 기본 지름은 540px다. 두 초상의 크기·위치·테두리는 호버와 무관하게 고정한다.
- 각 인물의 눈 감음/눈 뜸 RGBA PNG를 alpha 교차한다. 선택은 이름 아래 `선택된 기록`과 하단 패널/시작 버튼으로 구분하며, 호버는 설명 미리보기만 바꾼다.
- 원본 1254×1254 네 장은 `Assets/Resources/Art/Generated/*-profile-eyes-*-v11.png`다. 기존 이안 v2·루나 v6는 지도·전투용으로 보존한다.
- `PortraitMedallion.shader`는 이제 블러나 색 조작 없이 원형 alpha만 굽는다. 표시본 네 장과 Material은 View가 해제한다.
- Presentation/아트만 바뀌어 저장 규칙은 10을 유지한다. 상세 프롬프트·투명도·검증은 `Docs/CHARACTER_SELECTION_V11.md`를 따른다.
- 검증 기준: EditMode 140/140, Full HD/720p fixture 27장, Runtime errors 0. 기본 두 눈 감음·이안 호버·루나 호버를 각각 캡처한다.

## 재개용 인계 · 전장·카드 UI v10 · 2026-09-10

사용자가 제공한 스케치를 바탕으로 캐릭터 선택, 카드 정보 구조, 전투 화면과 지형 변수를 개편했다. 아래 v9 인계보다 이 항목이 최신이다. 탑형 지도와 스토리는 사용자가 나중에 함께 구체화하자고 했으므로 자동 구현하지 않는다.

- 중앙 관문 양쪽의 원형 이안·루나 초상. 비활성은 흐림/저채도, 호버는 약 8% 확대·선명화한다. 기존 초상 원본을 덮어쓰지 않는다.
- 전투는 전체 화면 먹빛 필드 배경, 1180×944 활성 좌표 영역, 가장자리 HUD다. 실제 좌표 `x=0~10`, `y=-4~4`와 카드 수치는 유지한다.
- 손패는 아래 부채꼴이며 호버 시 짙은 책장형 확대 카드가 열린다. 카드·플레이어/적 상태·지형 키워드에 상세 툴팁을 제공한다. 참고작의 자산이나 프레임은 포함하지 않았다.
- 생성 전투의 기록 기둥은 이동을 막고, 굴절 프리즘은 작도선 교차 시 그 방출의 적중 피해 +2를 대상마다 적용한다. 여러 프리즘은 중첩하지 않는다. 일반·정예 지형 2개, 보스 3개를 시드로 재현한다.
- 새 Core는 `BattleTerrain.cs`, 새 UI 보조는 `GraphaclysmBattlefieldView.cs`, 원형 초상 셰이더는 `Resources/PortraitMedallion.shader`, 회귀는 `BattlefieldV10Tests.cs`다. 상세 계약은 `Docs/BATTLEFIELD_V10.md`를 따른다.
- 생성/전투 규칙이 바뀌어 저장 형식 1의 `RulesVersion`을 10으로 올렸다. v9 원정 저장을 새 규칙으로 재생하지 않는다. 설정 파일은 유지한다.
- 검증: Unity EditMode **140/140**, Full HD/720p fixture **26장**, Runtime errors **0**. 로그는 `Logs/editmode-battlefield-v10.xml`, `Logs/battlefield-v10-smoke-final.log`, 캡처는 검증 사본의 `Logs/BasicsV9Captures`다. Windows 빌드·직접 플레이·Profiler·전체 3층 밸런스는 v10에서 재검증하지 않았다.
- 원본 Unity Editor는 열려 있다. 재검증은 `Logs/CombatV2VerificationProject`에서 하며 사용자 Editor를 종료하지 않는다. 현재 fixture는 기존 `BasicsV9Smoke.RunBatch`를 확장해 사용한다.

## 재개용 인계 · 기본 기능 v9 · 2026-09-07

사용자가 저장·이어하기와 게임의 기본 기능 보완을 요청했고, 구현·검증을 마친 뒤 여기까지 작업하고 인계를 남기도록 요청했다. 현재 작업은 마무리한다. 재개 시 최신 사용자 요청을 먼저 확인하며, 아래 미구현 후보를 자동으로 구현하지 않는다.

아래 v8 인계보다 이 항목이 최신이다. 기본 환경은 v9, 카드·유물·전투 수치와 3층 던전은 v8을 유지한다.

- 자동 저장·이어하기, 원정 교체 확인, Esc 일시정지·저장 후 타이틀/종료, 음량·화면·연출 설정, 원정 중 D 덱/유물 보기, 6종 합성 효과음, 첫 전투에 열리는 5단계 조작 안내를 추가했다.
- 안내는 페이지형 설명이다. 실습형 튜토리얼·상점·카드 강화·배경음악은 아직 없다. 이를 완료했다고 표현하지 않는다.
- 저장은 `RunJournal.cs`의 시드/캐릭터/성공한 Run 명령 재생이다. `RunSaveStore.cs`는 형식 1/규칙 9, SHA-256, 같은 폴더 임시 기록/교체/백업 복구를 담당한다. **수치·카탈로그·생성기·셔플·명령 순서를 바꾸면 규칙 버전을 올리거나 마이그레이션을 구현한다.** 새 상태 변경 명령은 기록과 Replay에 함께 추가한다.
- 기본 경로는 `Application.persistentDataPath`, 현재 Windows `%USERPROFILE%/AppData/LocalLow/DefaultCompany/GRAPHACLYSM`이다. 제품/회사명 변경 시 저장 이전을 고려한다. 사용자 저장을 테스트로 덮어쓰지 않는다.
- `GraphaclysmSystemView.cs`는 메뉴·설정·저장 연결, `GameAudio.cs`는 View 수명의 재사용 AudioSource/Clip을 소유한다. 별도 화면 시계를 멈추어 작도 중에도 일시정지한다. 도움말·설정·덱·확인 창도 입력/진행을 막는다.
- `SaveAndPreferencesTests.cs`에서 저장·복원·취소·응축·보상·방·2층 전이·손상 복구/쓰기 실패/종료 상태·설정을 확인한다. `BasicsV9Smoke.RunBatch`는 실제 저장을 별도 `Logs/BasicsV9Captures/UserData-*`에서 검사한다. 일반 batch 실행은 사용자 저장을 끈다.
- 상세 실행·제약은 `Docs/BASICS_V9.md`를 따른다. 원본 Unity가 열려 있으므로 이후 검증은 기존 `Logs/CombatV2VerificationProject` 사본에서 수행한다. 사용자 Editor를 종료하지 않는다.
- 최종 v9 검증: EditMode **137/137**, Full HD/720p **22장**, Runtime errors **0**. 결과는 `Logs/editmode-basics-v9.xml`, `Logs/basics-v9-smoke-final.log`, `Logs/CombatV2VerificationProject/Logs/BasicsV9Captures/smoke-result.txt`다. 전체 3층 밸런스/새 Profiler 결과로 표현하지 않는다.
- Windows 빌드 `Logs/Builds/GraphaclysmV9/GRAPHACLYSM.exe` 성공, 오류/경고 0. 검증 사본에만 Unity가 생성한 설정 5개 차이가 있으며 소스 스크립트/asmdef는 전부 일치한다. `Logs/basics-v9-source-verification.txt` 참조. 자동 생성 설정을 원본에 덮어쓰지 않는다.

### Git 상태와 재개 순서

- 마감 시 브랜치는 `main`, 마지막 커밋은 `4bac41d`다. **v9 코드·새 파일·.meta·대표 화면·문서는 로컬 작업 트리에 있으며 아직 커밋·푸시하지 않았다.** 원격의 v8 기준 이력과 혼동하지 않는다.
- 먼저 `git status --short --branch`와 최근 이력으로 이후 사용자 변경을 확인한다. 미추적 v9 파일까지 보존하며, 현재 변경을 초기화하거나 검증 사본으로 덮어쓰지 않는다.
- 다음 커밋도 기존 SSH 서명을 사용한다. 마감 시 `gpg.format=ssh`, `commit.gpgsign=true`, `user.signingkey=~/.ssh/id_ed25519_signing.pub`를 확인했다. 커밋할 때 새 소스·.meta·대표 화면·문서를 함께 포함하고 Logs/빌드/사용자 저장은 제외한다. 푸시할 경우 아래 서명 지침대로 GitHub의 Verified까지 확인한다.
- 기능/파일 설명과 재검증 명령은 `Docs/BASICS_V9.md`, 플레이 방법은 `README.md`와 `PROTOTYPE.md`를 읽는다. Unity 사용 여부는 재개 시 다시 확인한다. 이번 마감은 문서만 변경하므로 게임 테스트를 다시 실행하지 않는다.

### 다음에 확인할 사항

- Windows 실행 파일의 직접 플레이, 실제 음향 청취, 창/전체 화면·해상도 변경의 수동 확인은 아직 하지 않았다. Editor 화면 fixture 검증과 Windows 빌드 성공을 이 확인까지 완료한 것으로 표현하지 않는다.
- 실제 생성 원정 두 시드에서 2층 첫 전투까지 저장 복원을 확인했지만, 전체 3층의 최종 밸런스와 신규 Profiler 측정은 미완료다. 사용자 플레이 피드백을 받아 조합 반복성·판독성·응축의 가치·후반 난이도를 검토한다.
- 상점·카드 강화·실습형 튜토리얼·배경음악은 후속 후보다. 현재 구현된 5페이지 안내와 기본 효과음을 이 기능들로 확대 해석하지 않는다.

## 이전 인계 기록 · v8 · 2026-09-06

아래는 v8 완료 당시의 인계다. 최신 상태와 다음 작업 판단에는 위 v9 인계를 우선한다. v5~v7 검증 항목도 이전 기록이다.

### 완료 상태

- 파편 카드 23종·유물 14종·3층 생성 던전. 층마다 보스 포함 8개 방, 한 경로 24개 방이다. 카드 사전·이벤트/휴식/유물 방·이동 취소·궁극기가 구현되어 있다.
- v8 새 카드: 쌍성직조(가로/세로 2:3 재샘플링), 월식(4배속 궤적 차감), 편월(한쪽 반경 변조), 비껴쓴 선(전단), 엇박자(가로 위상 이동/드로우), 삼중봉인(복소 세제곱). 연구 출처·정확한 계수·부가 능력은 `Docs/CARD_RESEARCH_V8.md`를 따른다.
- 새 카드도 이전 궤적 **전체**를 감싼다. 계산기/직접 문자 입력/시작 전용 카드/에너지 비용을 다시 넣지 않는다. 원형 기본 궤적에 삼중봉인을 쓰면 원을 세 번 지나고, 엇박자는 대각선이 된다. 문양을 모든 조합의 실제 출력이라고 설명하지 않는다.
- 화면 필드는 950×760, 동일 축척 95px/단위로 면적 56.25% 확대했다. **화면상의 확대**이며 월드 범위 x=0~10, y=−4~4는 그대로다. 좌측 조립·이동, 우측 적 의도·상세·행동, 중앙 아래 손패다.
- 응축의 과도한 보상은 v7에서 조정했다. 첫 체력 2/두 번째 4, 보호막 무시, 생존 체력 필요, 방출/해체 사이 최대 2번, 기본 1장+추가 최대 1장 보충이다. 새 카드 때문에 이 제한이 우회되지 않게 한다.
- 이안 v2·루나 v6·배경 v3 유지. 루나는 긴 흰머리와 밤 보라 고스 복장, 이안과 반대 방향이다. 타이틀에는 인물을 표시하지 않는다. 전투의 정보 과밀과 반복성을 줄이는 것이 사용자의 지속적인 우선순위다.

### 다음 작업 때 볼 위치

- 수학: `Assets/Scripts/Core/Equations/FragmentEquation.cs`
- 카드 수치/효과: `Assets/Scripts/Core/Cards/FragmentCardCatalog.cs`
- 시작 덱: `Assets/Scripts/Core/Characters/PrototypeCharacterCatalog.cs`
- 전투 화면: `Assets/Scripts/Runtime/Presentation/GraphaclysmModernView.cs`, `GraphaclysmModernBattle.cs`, `GraphaclysmQuietView.cs`, `GraphaclysmFragmentView.cs`, `GraphaclysmAtlasView.cs`
- 신규 회귀: `Assets/Tests/EditMode/Combat/WideV8Tests.cs`; 기존 응축/유물/층 회귀는 `ExpeditionV7Tests.cs`
- 화면 fixture: `Assets/Editor/WideV8Smoke.cs`; 시작 메서드는 `Graphaclysm.Editor.WideV8Smoke.RunBatch`

### 검증과 남은 확인

- 마지막 코드 검증: Unity EditMode **122/122 통과**, Full HD/720p fixture **27장**, Runtime errors **0**. 원본과 격리 검증 사본의 대상 108개 파일 일치를 확인했다.
- 로컬 기록: `Logs/editmode-wide-v8.xml`, `Logs/WideV8Captures/smoke-result.txt`, `Logs/wide-v8-smoke-final.log`, `Logs/wide-v8-source-verification.txt`. 검증 사본은 `Logs/CombatV2VerificationProject`다. 이 경로들은 Git에서 제외하므로 다른 PC에는 없을 수 있다.
- 저장소에 포함한 대표 화면: `Docs/Screenshots/battle-v8.png`. 실행과 재검증 방법은 `README.md`, `PROTOTYPE.md`에 있다. Unity 6000.3.23f1에서 `Assets/Scenes/SampleScene.unity`를 연다.
- 전체 3층 런의 최종 밸런스나 신규 Profiler 검증을 완료한 것은 아니다. 사용자의 다음 플레이 피드백을 받아 새 조합의 반복성/판독성, 응축 체력 대비 드로우, 후반 적 난이도를 우선 조정할 수 있다. 필요하면 카드 사용 전후 궤적 안내·단계형 튜토리얼을 검토한다.
- 저장·상점·카드 강화·단계형 튜토리얼·신규 음향은 미구현 후보이며 확정 작업 목록이 아니다. 이번 인계는 문서 변경만 하므로 게임 테스트를 다시 실행하지 않는다.

### 저장소와 서명 — 필수

- 원격 `https://github.com/oscar87657/GRAPHACLYSM.git`, 기본 브랜치 `main`. GitHub 초기 README 커밋을 부모로 보존했다.
- unsigned 업로드 `d169077`은 서명된 `26bcf10`으로 교체했다. `26bcf10`은 GitHub API에서 `verified=true`, `reason=valid`를 확인한 v8 기준 커밋이다. 이후 문서/개발 커밋은 그 위에 정상 push한다. 예전 unsigned 커밋으로 되돌리거나 재게시하지 않는다.
- **사용자는 Verified 서명을 필수로 요구했다.** 이 PC의 저장소 로컬 설정은 `gpg.format=ssh`, `commit.gpgsign=true`, 이메일 `127852551+oscar87657@users.noreply.github.com`이다. 기존 GitHub 등록 키 `~/.ssh/id_ed25519_signing.pub`를 사용한다. 로컬 검증용 allowed signers는 `.git/allowed_signers`에 있다.
- 로컬 Git 설정/키는 clone으로 전달되지 않는다. 다른 PC에서는 해당 사용자의 GitHub 등록 서명 키를 확인하고 설정한 뒤 커밋한다. 비밀키를 복사하거나 저장소에 추가하지 않는다.
- push 후 GitHub 커밋 API의 `commit.verification.verified=true`까지 확인한다. 비공개 저장소 조회는 인증 없이 404가 날 수 있다. 기존 GitHub 로그인을 사용하되 토큰/credential 출력을 로그나 대화에 노출하지 않는다.
- 재개 시 먼저 `git status --short --branch`와 최근 이력/서명 설정을 확인한다. 사용자 변경을 보존한다. 서명 재작성은 이번에만 명시적으로 요청되었고, 앞으로는 정상 push가 기본이다. 당시 교체는 기대한 원격 SHA를 지정한 force-with-lease로 보호했다.

## 프로젝트와 방향

- Unity 6000.3.23f1, Windows PowerShell. Git 저장소이며 원격은 `https://github.com/oscar87657/GRAPHACLYSM.git`, 기본 브랜치는 `main`이다.
- 한국어로 소통하고 결과·다음 확인 사항을 간결하게 알린다.
- 핵심은 작은 수식 조각을 조립하여 적을 맞히고, 같은 필드의 자신을 맞혀 버프를 받는 덱 빌딩 로그라이트다.
- 캐릭터는 이안과 흰머리 동생 루나. 과거의 아르카·노아 이름을 새 콘텐츠에 사용하지 않는다.
- 아트는 아르케아의 빛·파편·여백을 중심으로, 리버스 1999의 재료감·회화적 명암을 참고한다. 공식 게임 이미지는 조사 자료로만 두며 런타임 자산으로 넣지 않는다.
- 현재 규칙·화면은 v8 조사 기반 파편 확장·전투 필드 확대이며 v7의 카드 사전·기준점 이동·3층 원정을 유지한다. 이안 v2·루나 v6 초상과 v3 배경을 사용한다. 메인 타이틀에는 캐릭터를 표시하지 않는다. 루나는 앳된 인상·긴 흰머리·밤 보라색 고스 드레스·레이스·천문 작도륜을 사용하며 오른쪽을 바라본다. 이안은 왼쪽을 바라본다. 사용자의 이안 참고 원본을 덮어쓰지 않는다. 최종 도트 제작 등 납품 형태는 별도 결정 사항이다.
- 이전 지침·기획·아트는 `Docs/Archive/BeforeCombatV2`와 `Logs/BeforeCombatV2-20260906`에 보관되어 있으며 현재 요구를 대체하지 않는다.

## 문서

- `GAME_DESIGN_DOCUMENT.md`: 전체 기획, 구현·방향·제안 구분
- `COMBAT_REDESIGN.md`: 전투 개편의 수치·순서·향후 전술 요소
- `PROTOTYPE.md`: 실행·조작·검증·현재 한계
- `ARCHITECTURE.md`: 소유권·의존성·메모리 규칙
- `ART_DIRECTION.md`, `Docs/ART_PROMPTS_V6.md` (이전 루나 V4, 배경 V3, 이안 V2 기록도 보관): 조사 출처·새 시안·생성 프롬프트
- `PERFORMANCE.md`: 기존 GL 배치 출력의 변경 전 측정 기록

## 의존성과 소유권

`Runtime/Presentation → Application → Core`를 지킨다. Core는 Unity API를 참조하지 않는다. 콘텐츠 정의는 불변이며 체력·에너지·좌표·상태를 화면이 직접 수정하지 않는다. 상태 변경은 Try 명령 또는 명시적 전이를 통한다. 전역 변경 가능 싱글턴을 추가하지 않는다. 조립 루트는 Bootstrap이다.

BattleSession은 수식·적·TacticalCombatState를 소유한다. 플레이어 좌표·공명·궁극기는 TacticalCombatState, 상태 수치와 수명은 CombatStatusState가 가진다. RunGameSession은 손패와 수식의 트랜잭션, 맵·보상·지속 체력·공명을 조정한다. 진단용 상태 설치는 Editor 전용으로 제한한다.

## 현재 규칙

- 타이틀/카드 사전 → 이안/루나 → 3층 생성 던전. 층마다 보스 포함 8개 방을 방문하고 전체 경로는 24개 방이다. 전체 생성 노드는 45~66개. 매 층 3번째 방 비전투, 5번째 유물, 8번째 보스. 1·2층 보스 뒤 체력 8 회복·유물 보상 후 다음 층, 3층 보스 뒤 완주.
- 실제 카드 풀은 FragmentCardCatalog 31종. 시작 덱 12장·시작 손패 5장·보존 손패 8장. 시작 카드/계산기 입력 없음. 모든 파편이 첫 카드로 가능하다.
- 각 카드의 빈칸은 앞선 복소 평면 궤적 전체 F(t)다. 공통 F₀=1.6exp(it)를 기준으로 역회전·궤적 회전·반경 변조 등을 중첩한다. 전장 좌표는 O+F(t), 새 조립의 O는 첫 카드 사용 순간 플레이어 좌표다. 귀환점은 사용 당시 플레이어 위치로 O를 옮기고 서쪽의 문은 O.x−2, 승천은 O.y+1.5다. 기준점은 필드 내부로 제한하며 이후 변형은 F에 적용한다. 빈 조립대는 표시/공격하지 않는다.
- 카드·이동 에너지 비용 없음. 방출/해체 기본 보충 2장, 응축 기본 1장. 손패 유지. 예약 파편은 최대 8개이며 드로우/버린 더미에서 분리한다.
- 응축은 식/손패 유지·파편 확정·적 행동·보충, 방출/해체 사이 최대 2번. 첫 체력 2, 두 번째 체력 4를 보호막과 무관하게 소비하고 생존 체력이 부족하면 거부한다. 응축 추가 드로우는 합계 최대 1장, 일반 턴 드로우 유물 미적용. 방출은 적중과 능력 처리 뒤 조립대 초기화, 남은 손패 보존. 해체는 공격/파편 추가 드로우 없이 예약을 버리고 적 행동·기본 보충.
- 드로우 파편은 다음 보충에 한 번만 적용하고 넘치는 보충은 저장하지 않는다. 응축 이후 새 파편만 취소 가능. 확정된 이전 파편 취소·예약 카드 재셔플·드로우 복제를 막는다.
- 조립 위력은 카드마다 0~3으로 다르고 합계 최대 10이다. 이동·지원 파편 일부는 추가 위력 0이며 슬롯 수만으로 피해를 얻지 않는다. 대상당 한 번 판정. 슬롯/응축/손패가 준비량의 제한이며 기존 잉크 64 제한은 현재 모드에서 사용하지 않는다.
- 실제 곡선은 층별 고정 버퍼, 1,536개 선분, 최대 주파수 96. 표시·적/자신 판정은 같은 Sample을 사용한다. 기존 Calculator/Skill/Spell 풀은 legacy 회귀용이다.
- 이벤트 6종. 비치명적 체력 비용, 회복/공명/카드/유물/덱 제거. 제거 최소 덱 크기는 기본 손패, 취소 시 비용 없음. 모든 비전투 방은 지나가기 가능.
- 유물 보상은 미보유 수가 1~2개면 그 수만 제공한다. 방·보상 완료는 한 번만 진행한다.
- 수식과 손패의 이력 버퍼는 `BattleSession.MaximumPlayedCards`(11)를 사용한다. 상수 9로 되돌리지 않는다.
- 플레이어 초기 (4,-2), 반경 0.48, 중심 반경 1.8 안의 좌표 클릭 이동을 턴당 한 번, 비용 없음. 범위 밖은 경계, 적·기둥 겹침은 가까운 빈 지점으로 보정한다. 작도 전 이동 취소는 위치·이동 기회를 복구한다. legacy 유료/경쾌 취소는 기존 회귀에 남긴다. 카드 취소와 별개다.
- 자기 그래프 적중은 기본 보호막 3 + 사용한 기술의 자신 능력. 적·자신 모두 한 작도에 대상당 한 번 처리한다.
- 보호막·집중·재생·잔불·약화·노출·고정·경쾌. 고정 크기 8 배열, 수치 상한 24. 집중·노출의 새 적용은 이번 피해를 소급 변경하지 않는다.
- 적 조준점은 턴 시작의 플레이어 좌표에 고정된다. 일반 반경 0.85, 맹공 1.65. 축적 턴 보호막 12, 체력 피해를 준 일반 공격은 약화, 맹공은 잔불을 준다.
- 공명 최대 6, 적 2명 이상 적중 +1, 적·자신 동시 적중 +2. 준비 취소 가능, 실제 작도에 소비, 전투 사이 유지.
- 이안 궁극기 피해 +6·이번 적 이동 봉쇄. 루나 궁극기 자신 적중 반경 1·자가 적중 시 추가 정화/보호막 8/회복 5.
- 직접 피해와 잔불 승리 모두 공통 CompleteVictory로 보상·맵을 진행한다.
- 등급 한국어명은 일반/고급/희귀/전설, 색은 은회색/청록/라벤더/창백한 금색. 보상 가중치는 100/45/15/3의 잠정값이다.
- 현재 FragmentRelicCatalog 유물 20종은 기존 보충·시작 손패·피해·승리 회복에 시작 보호막/공명·자가 적중 보호막·첫 방출·짧은/긴 조립 피해·이동 후 방출 보호막·승리 공명과 v12 조건 유물을 더한다. 에너지 유물은 현재 풀에서 제외했다. ID 중복 획득 불가, 다른 ID의 같은 효과는 합산.

## 수학과 출력

기본 화면은 `GraphaclysmModernView`와 Battle/Screens/FragmentView/QuietView/AtlasView partial (CalculatorView에는 legacy 입력과 방 화면이 남음). `AstralUi`는 Orbit·Pretendard·Cormorant를 재사용하고, `AstralSpellRenderer`는 중심 공개·발광선을 출력한다. 필드는 정면, 두 축 단위당 95 논리 px. 카드 앞면에 상세 수식·분류·지속시간을 다시 나열하지 않는다. 적 행동 정보는 오른쪽, 파편 조립대·자원은 왼쪽에 둔다. 작도 1.08초에 한 번 판정하고 1.85초에 적 행동으로 진행한다. legacy 화면은 명시적 Editor 진단에만 사용한다.

기존 `PrototypeCardCatalog`의 완성 그래프 18종·변환 4종은 수학 보관함이며 현재 시작 덱·보상에서 사용하지 않는다. 현재 파편은 이전 궤적을 감싸며 1,536개 선분의 같은 기하로 표시·적/자신 명중한다. 계산기 768개 선분은 legacy 모드다. legacy 입력 u=x-5/θ와 160개 곡선 선분은 회귀 코드에만 남긴다. 화면 밖·정의되지 않은 값은 자르거나 제외한다. 수식을 화면에 맞추려고 임의 변형하지 않는다.

폴리오미노와 폴리노미오그래프는 다르다. 폴리노미오그래프는 이미지 텍스처가 아니라 **실제 수식의 선**이다. p(z)=z^n-1의 뉴턴 수렴, 64×48 필드, 근 경계와 반복 4/6/8/11/15/20의 등고선을 마칭 스퀘어로 만들며 최대 8,192개 선분 버퍼를 사용한다. GL 렌더러와 EquationAnalyzer가 같은 선분을 읽는다. 영역 이미지 공격으로 되돌리지 않는다.

작도는 현재 기준점 O에서 확장되는 반경으로 실제 선분을 공개한다. legacy는 (5,0)이다. 공개 반경은 O부터 가장 먼 필드 모서리까지로 계산한다. 얇은 전체 예측선은 남는다. 장식과 적중 파편은 판정에 참여하지 않는다. 기존 순차 공개 A/B 프로파일은 변경 전 비교 기록이다.

## 메모리·품질

- Update, OnGUI, 샘플링, 충돌 경로에서 LINQ·클로저·임시 컬렉션·문자열 조합을 만들지 않는다.
- 매 프레임 Instantiate/Destroy를 하지 않는다. 고정 배열과 재사용 버퍼를 우선한다.
- 새 할당은 소유 주체·수명·최대 용량을 ARCHITECTURE에 기록한다.
- 화면 문자열은 명령 후 갱신한다. 상태 문자열은 최대 8종, 툴팁 배열은 손패 용량이다.
- Core 규칙 변경에는 의미 있는 EditMode 회귀 테스트를 추가·갱신한다.
- 성능 개선은 Profiler로 검증한다. 이전 측정 수치를 새 전투 전체의 성능처럼 표현하지 않는다.

## 검증

2026-09-06 v5: Unity EditMode 97/97 통과. `Logs/editmode-fragments-v5-final.xml` 참조. Full HD·720p 24장과 runner 결과는 `Logs/FragmentsV5Captures`에 있다. 재현용 손패·HP·방 fixture이므로 전체 런 밸런스 통과를 뜻하지 않는다. 100개 시드의 생성 경로 접근성은 Core 테스트에서 확인한다.

Unity가 이 프로젝트를 열고 있으면 강제 종료하지 말고 Test Runner 또는 격리된 검증 복사본을 사용한다. `Logs/CombatV2VerificationProject`는 이번 검증을 위한 Assets/Packages/ProjectSettings 복사본이며 영구 소스의 기준이 아니다. 비교 후 실행하고 사용자 작업을 복사본으로 덮어쓰지 않는다.

과거 `Temp/GraphaclysmVerification`, `Temp/GraphaclysmEditModeHarness` 보조 프로젝트는 없다. Temp에 영구 소스를 복구하지 말고 Unity Test Runner를 사용한다. 현재 화면 자동 캡처는 `Graphaclysm.Editor.WideV8Smoke.RunBatch` (V5/V6 fixture는 이전 규칙 기록); 명시적인 batch 호출로만 실행한다. 실제 실행 명령은 PROTOTYPE을 따른다.

## 다음 작업 후보

1. 사용자 플레이 피드백으로 수식 반복성·응축 횟수·보충량·조립 보너스·이동 회피율·기술 가치·정예 난이도 조정.
2. 단계형 튜토리얼과 카드 간 전후 변화 안내.
3. 필요한 전술 요소부터 봉쇄 지대·밀치기·카드 보존 등을 도입.
4. 고급 수학 곡선을 조립 문법으로 확장. 적응형 샘플링과 비용·복잡성 제한을 함께 검토.
5. ScriptableObject 콘텐츠 어댑터, uGUI, 저장·상점·카드 강화.

새 사용자 요청이 있으면 위 후보보다 먼저 처리한다.

이번 v3 이전 문서는 `Docs/Archive/BeforeRoguelikeV4`, 작업 전 소스는 `Logs/BeforeRoguelikeV4-*`에 보관했다. V2/V3 smoke는 역사적 고정 맵 fixture이며 현재 생성 런 검증 도구로 혼용하지 않는다.

v7 변경 전 사본은 `Logs/BeforeExpeditionV7-20260906-140614`, 문서는 `Docs/Archive/BeforeExpeditionV7`에 있다. 최신 v8 변경 전 사본은 아래 v8 기록을 따른다. v5 이전 기록도 보관한다. V4의 계산기/에너지/잉크 설계는 최신 사용자 수정으로 대체되었다.

## v6 표시 기록 (현재는 아래 v8까지 적용)

기본 전투에는 체력·공명·조립 순서·적의 다음 행동·카드 이름/기호·방출/응축을 우선한다. 카드 능력은 호버로 확인하고 드로우 보너스만 기본 배지로 남긴다. 보상·덱 제거의 넓은 카드는 능력 요약을 유지한다. 상태·좌표 숫자·수식 열기는 상단 `상세`, 적 상태와 예상 피해는 적 호버로도 확인한다. 궁극기 설명은 호버/상세에, 해체는 `···`에 둔다. 새 피드백은 3초 표시하며 반복 안내를 상시 노출하지 않는다.

V6 Full HD·720p 화면 17장과 Runtime errors 0은 `Logs/QuietV6Captures`와 `Logs/quiet-v6-smoke-final.log`에 있다. Core/Application은 변경하지 않았으며 97개 EditMode는 v5 통과 기록이다. 이번에는 UI fixture를 다시 실행했다.

## v7 표시와 검증

메인 카드 사전은 현재 파편 23종과 유물 14종을 보여 준다. 직조/변형/이동/지원 필터, 8개씩 페이지, 상세 효과·수식·첫 카드 실제 곡선 예시를 제공한다. 카드 문양은 코드로 그린 상징이며 실제 출력 예시는 별도다. 카드 앞면은 이름·문양·기호·짧은 동작/역할·위력 또는 드로우 배지다. 능력 전체는 호버/사전/보상에서 읽는다. 이동 화살표는 왼쪽 하단 한 줄, 이동/파편 취소는 같은 행이다. 해체 보조 버튼은 오른쪽 아래, 응축 비용과 드로우는 상시 보인다.

Unity EditMode 111/111 통과: `Logs/editmode-expedition-v7.xml`. V7 실제 화면 23장: `Logs/ExpeditionV7Captures`. 신규 Profiler 또는 전체 런 밸런스 통과 주장은 하지 않는다.

## v8 파편 확장과 넓은 전투 화면

리사주·에피트로코이드·리마송·전단 변환·위상 차·복소 거듭제곱을 조사하여 파편 6종을 추가했다. 실제 카드는 총 23종이며 기존 14종 유물과 3층 원정을 유지한다. [조사와 정확한 합성 규칙](Docs/CARD_RESEARCH_V8.md)을 따른다. 이안 시작 덱은 중복 잔향 한 장을 비껴쓴 선으로, 루나는 윤무 한 장을 쌍성직조로 교체했다.

필드 사각형은 (423,108)에서 950×760, 두 축 모두 95 논리 px/단위다. 이전보다 가로·세로 25%, 면적 56.25% 확대했다. 전장 좌표 범위와 이동/명중 규칙은 유지한다. 왼쪽 자원·조립·이동, 오른쪽 적 의도·호버·행동, 필드 아래 손패로 정렬했다. 상세/수식 버튼과 피드백은 필드 위쪽이다. 최대 8장 손패는 폭을 나누어 배치하고 호버를 확대하며, 카드 설명과 배지 사이 간격을 확보한다. 새 카드 문양도 기존 코드 선을 사용한다.

Unity EditMode 122/122 통과: `Logs/editmode-wide-v8.xml`. 새 수식이 첫 카드·이전 비원형 궤적에 적용되는 실제 좌표와 기준점/취소/주파수 경계를 확인한다. 화면 검증은 명시적 `Graphaclysm.Editor.WideV8Smoke.RunBatch`로 실행하며 결과는 PROTOTYPE의 최신 검증 항목을 따른다. 전체 런 밸런스·새 Profiler 측정을 대신하지 않는다.

V8 최종 화면은 `Logs/WideV8Captures` 27장, Runtime errors 0이다. `Logs/wide-v8-source-verification.txt`에 검증 사본과의 일치를 기록했다. 변경 전 소스는 `Logs/BeforeWideV8-*`, 문서는 `Docs/Archive/BeforeWideV8`에 보관했다.

## Git 관리

Unity 소스·에셋과 .meta, Packages, ProjectSettings, 문서를 함께 커밋한다. .gitignore에 따라 Library/Temp/Logs/UserSettings/빌드/IDE 개인 설정과 Docs/ArtReferences의 조사 원본은 제외한다. Logs의 격리 검증 복사본이나 백업을 저장소에 추가하지 않는다. 대표 화면은 Docs/Screenshots에 둔다. 기존 원격 이력을 보존하며 기본적으로 force push를 사용하지 않는다.

이 프로젝트의 커밋은 반드시 GitHub에서 Verified로 확인되는 서명을 포함한다. GitHub에 등록된 기존 SSH 서명 키와 계정에 연결된 이메일을 사용하고 저장소 로컬 commit.gpgsign=true를 유지한다. 서명 키의 비밀키나 자격 증명을 저장소에 넣지 않는다. 푸시 후 GitHub의 verification.verified=true를 확인한다.
